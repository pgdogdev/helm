# Test multiple passwords per user
databases:
  - name: primary
    host: postgres-primary.example.com
    port: 5432

users:
  - name: app_user
    database: primary
    passwords:
      - "one"
      - "two"
  - name: admin_user
    database: primary
    password: single_password
