# Test ExternalSecrets integration
externalSecrets:
  enabled: true
  secretStoreRef:
    name: my-secret-store
    kind: SecretStore
  remoteRefs:
    - secretKey: users.toml
      remoteRef:
        key: pgdog/users
        property: users.toml
